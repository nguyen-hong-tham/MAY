{
  "version": 2,
  "projects": [
    {
      "src": "MAY",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist",
        "buildCommand": "npm install && npm run build"
      }
    },
    {
      "src": "MAY-admin",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist",
        "buildCommand": "npm install && npm run build"
      }
    },
    {
      "src": "may-api",
      "use": "@vercel/node",
      "config": {
        "includeFiles": "dist/**"
      }
    }
  ]
}
